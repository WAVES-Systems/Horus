<UserControl x:Name="Uct_round" x:Class="Waves.Horus.Controls.RoundButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Waves.Horus.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MouseEnter="Uct_round_MouseEnter" MouseLeave="Uct_round_MouseLeave" GotFocus="Uct_round_GotFocus" LostFocus="Uct_round_LostFocus" MouseLeftButtonDown="Uct_round_MouseLeftButtonDown" MouseDown="Uct_round_MouseDown" MouseUp="Uct_round_MouseUp">
    <Grid>
        <Border x:Name="Brd_border" BorderThickness="2" Grid.Row="0" Grid.ColumnSpan="2"
        CornerRadius="22" BorderBrush="#FFCDCDCD" Opacity="0.8"/>
        <Border x:Name="Brd_background" Grid.Row="0" Grid.ColumnSpan="2"
        CornerRadius="22" Background="#FFCDCDCD" Opacity="0"/>
        <TextBlock x:Name="Txb_caption" TextWrapping="Wrap" Text="Loading..." Margin="0" Foreground="#FFCDCDCD" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="17"/>

    </Grid>
</UserControl>
